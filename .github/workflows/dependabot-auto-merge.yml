name: "🤖 Dependabot Automation"

# Automated dependency management with intelligent merge policies
# Triggers: Dependabot PR opened/updated/reopened
# Purpose: Auto-merge safe updates (patch + dev minor), alert on major updates

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot-automation:
    name: "🤖 Dependabot: Smart Dependency Management"
    # Automatically handles dependency updates with safety checks and merge policies
    runs-on: ubuntu-latest
    if: ${{ github.actor == 'dependabot[bot]' }}
    steps:
      - name: "📊 Metadata: Extract Update Information"
        # Retrieves dependency update details (type, version change, dependency scope)
        id: metadata
        uses: dependabot/fetch-metadata@dbb158d5c31b2b8bd1617a99da8524c8b2ebe5cb # v2.4.0
        with:
          github-token: "${{ secrets.GITHUB_TOKEN }}"

      - name: "✅ Auto-merge: Patch Updates (Safe)"
        # Automatically merges patch version updates (bug fixes) as they are low-risk
        if: steps.metadata.outputs.update-type == 'version-update:semver-patch'
        run: |
          gh pr merge --auto --squash --delete-branch "$PR_URL"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_URL: ${{ github.event.pull_request.html_url }}

      - name: "🔄 Auto-merge: Dev Dependencies Minor Updates"
        # Auto-merges minor updates for development dependencies (tooling, testing)
        if: steps.metadata.outputs.update-type == 'version-update:semver-minor' && steps.metadata.outputs.dependency-type == 'direct:development'
        run: |
          gh pr merge --auto --squash --delete-branch "$PR_URL"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_URL: ${{ github.event.pull_request.html_url }}

      - name: "🚨 Alert: Major Version Update Detected"
        # Flags major updates for manual review as they may contain breaking changes
        if: steps.metadata.outputs.update-type == 'version-update:semver-major'
        run: |
          gh pr comment "$PR_URL" --body "🚨 Major version update detected. Please review manually before merging."
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_URL: ${{ github.event.pull_request.html_url }}
